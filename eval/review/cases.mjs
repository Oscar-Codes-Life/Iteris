// Synthetic fixtures, not historical bugs or measured review results.
const defects = [
 ['zero-default', 'Add an optional retry count. Preserve an explicit zero.', 'export const retries = n => n ?? 3;', 'export const retries = n => n || 3;', 'Explicit zero is replaced with three retries.'],
 ['pagination', 'Return exactly limit items from offset, including the first item.', 'export const page = (xs, offset, limit) => xs.slice(offset, offset + limit);', 'export const page = (xs, offset, limit) => xs.slice(offset + 1, offset + limit);', 'The first requested item is skipped.'],
 ['permission', 'Only active administrators may delete records.', "export const canDelete = u => u.active && u.role === 'admin';", "export const canDelete = u => u.active || u.role === 'admin';", 'An active non-admin or inactive admin can delete.'],
 ['sort-mutation', 'Return a sorted copy without changing the caller array.', 'export const sorted = xs => [...xs].sort((a,b) => a-b);', 'export const sorted = xs => xs.sort((a,b) => a-b);', 'The input array is mutated.'],
 ['money', 'Format integer cents as dollars with exactly two decimal digits.', 'export const dollars = cents => (cents / 100).toFixed(2);', 'export const dollars = cents => (cents / 10).toFixed(2);', 'All nonzero monetary values are ten times too large.'],
 ['expiry', 'Tokens are invalid at or after their expiry timestamp.', 'export const valid = (expiry, now) => now < expiry;', 'export const valid = (expiry, now) => now <= expiry;', 'A token remains valid at the exact expiry boundary.'],
 ['async-batch', 'The returned promise must settle only after all saves finish.', 'export async function saveAll(xs, save) { await Promise.all(xs.map(save)); }', 'export async function saveAll(xs, save) { xs.forEach(async x => await save(x)); }', 'The function returns before save operations finish.'],
 ['cache-key', 'Cache by tenant and record ID; tenant data must stay isolated.', 'export const key = (tenant,id) => JSON.stringify([tenant,id]);', 'export const key = (tenant,id) => String(id);', 'Identical record IDs collide across tenants.'],
 ['zero-id', 'Look up all non-null IDs, including numeric zero.', 'export const find = (id,db) => id == null ? null : db.get(id);', 'export const find = (id,db) => id ? db.get(id) : null;', 'ID zero is treated as absent.'],
 ['error-propagation', 'Return successful saves and preserve a rejected save as an error.', 'export async function persist(save) { return await save(); }', 'export async function persist(save) { try { return await save(); } catch { return true; } }', 'A failed save is reported as success.'],
 ['redaction', 'Remove password before returning a public user; do not mutate the input.', 'export function publicUser(user) { const {password,...safe}=user; return safe; }', 'export function publicUser(user) { return {...user}; }', 'The password leaks in the returned public user.'],
 ['unicode-length', 'Count Unicode code points, including a single count for emoji.', 'export const length = text => [...text].length;', 'export const length = text => text.length;', 'Surrogate-pair emoji are counted twice.'],
];
const bugs = defects.map(([id,requirement,before,after,expected]) => ({id,kind:'defect',requirement,before,after,expected}));
const clean = defects.map(([id,requirement,before]) => ({id:`${id}-clean`,kind:'clean',requirement,before,after:`// Preserve the documented contract.\n${before}`,expected:'No blocking finding. The implementation already satisfies the requirement.'}));
const acceptance = [
 {id:'missing-deduplication',kind:'acceptance',requirement:'Return uppercase names, remove duplicate names, and retain first occurrence order.',before:'export const names = xs => xs;',after:'export const names = xs => xs.map(x => x.toUpperCase());',expected:'The uppercase transformation is present, but duplicates remain.'},
 {id:'missing-cancel',kind:'acceptance',requirement:'Add loadWithSignal; reject immediately when the passed AbortSignal is already aborted.',before:'export const load = fetcher => fetcher();',after:'export const loadWithSignal = (fetcher, signal) => fetcher();',expected:'The signal is ignored, so an already cancelled call starts work.'},
 {id:'missing-empty',kind:'acceptance',requirement:'Return the average. Define the average of an empty array as zero.',before:'export const sum = xs => xs.reduce((a,b) => a+b,0);',after:'export const average = xs => xs.reduce((a,b) => a+b,0) / xs.length;',expected:'The empty input returns NaN instead of zero.'},
];
const structure = [
 {id:'duplicated-policy',kind:'maintainability',requirement:'Use a single canonical pricing policy for checkout and receipts. Preserve their outputs.',before:'export const price = n => n * 1.2; export const checkout = price; export const receipt = price;',after:'export const checkout = n => n * 1.2; export const receipt = n => n * 1.2;',expected:'The shared policy is duplicated. A useful finding proposes retaining the canonical function.'},
 {id:'identity-layers',kind:'maintainability',requirement:'Keep the identity transform direct and behavior-preserving.',before:'export const transform = x => x;',after:'const a = x => x; const b = x => a(x); const c = x => b(x); export const transform = x => c(x);',expected:'Point out unnecessary pass-through layers. Do not invent a functional defect.'},
 {id:'boolean-state',kind:'maintainability',requirement:'Represent each job as exactly one of queued, running or done, with a simple status function.',before:"export const status = job => job.state;",after:"export const status = job => job.isDone ? 'done' : job.isRunning ? 'running' : job.isQueued ? 'queued' : 'queued';",expected:'Independent booleans allow contradictory states; suggest retaining an explicit state model.'},
];
export const cases = [...bugs, ...clean, ...acceptance, ...structure];
