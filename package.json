{
  "name": "iteris",
  "version": "1.0.0",
  "description": "Autonomous GitHub ticket shipper powered by Claude Code",
  "type": "module",
  "bin": {
    "iteris": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "postbuild": "mkdir -p dist/github/commands && cp src/github/commands/code-review.md dist/github/commands/ && echo '#!/usr/bin/env node' | cat - dist/index.js > dist/index.tmp && mv dist/index.tmp dist/index.js && chmod +x dist/index.js",
    "dev": "tsc --watch",
    "typecheck": "tsc --noEmit"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "@octokit/rest": "^21.1.1",
    "ink": "^5.2.0",
    "react": "^18.3.1",
    "slugify": "^1.6.6",
    "terminal-image": "^4.2.0",
    "zod": "^3.24.2"
  },
  "devDependencies": {
    "@sindresorhus/tsconfig": "^7.0.0",
    "@types/node": "^22.13.10",
    "@types/react": "^18.3.18",
    "typescript": "^5.8.2"
  }
}
